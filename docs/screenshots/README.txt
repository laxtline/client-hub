====================================================================
 Screenshots for the GitHub README
====================================================================

Capture these 4 images from the running app (http://localhost:5173)
and save them in THIS folder with these exact file names so the
README picks them up automatically:

  1. login.png            -> the login page
  2. admin-dashboard.png  -> Admin dashboard (KPI cards + charts)
  3. kanban.png           -> a project's Kanban board (drag & drop)
  4. invoice.png          -> an invoice OR the AI progress report

TIPS
  - Log in as admin@demo.com / Demo@1234 (see DEMO_LOGINS.txt).
  - Toggle DARK MODE (🌙 in the navbar) and capture one shot in dark
    mode too — a light+dark pair looks very professional.
  - On Windows: Win + Shift + S to snip, then paste/save as .png here.
  - Keep them reasonably sized (e.g. 1280px wide) so the README loads fast.

After adding them, commit:  git add docs/screenshots && git commit -m "Add screenshots"
====================================================================
