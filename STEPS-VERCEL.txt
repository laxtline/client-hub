============================================================
  CLIENTHUB — GITHUB + VERCEL DEPLOYMENT (CURRENT STATE)
============================================================

Repo:      https://github.com/laxtline/client-hub
Vercel:    https://vercel.com/laxtline   (account: mymicrosoftacsurya-8635)
Frontend:  https://clienthub-surya.vercel.app       (project "clienthub",     root: client)
API:       https://clienthub-api-surya.vercel.app   (project "clienthub-api", root: server)

⚠️ VERCEL KI DO LIMITATION (code ki galti nahi, platform ki hai):
   ❌ Socket.io live notifications — page refresh pe dikhenge
   ❌ node-cron weekly AI report — button se manually generate hoga
   Baaki sab chalega: login, clients, projects, kanban, invoices,
   PDF, payments, AI summary (manual), dark mode.

------------------------------------------------------------
  ✅ JO HO CHUKA HAI
------------------------------------------------------------
1. Poora code GitHub pe push (main branch).
2. server/api/index.js — Express app Vercel function ban gaya.
3. server/vercel.json + client/vercel.json — routing/SPA rewrites.
4. CORS comma-separated CLIENT_URL list accept karta hai.
5. Winston file transport Vercel pe skip (warna function crash hota tha).
6. Vercel pe 2 project: clienthub (root=client, framework=vite) aur
   clienthub-api (root=server).
7. ✅ DONO project GitHub repo se CONNECTED — push karo, auto deploy hoga.
   (Verify ho chuka: last push se dono apne aap redeploy hue.)
8. Stable URLs assign: clienthub-surya + clienthub-api-surya.
9. Env vars set — API: NODE_ENV, CLIENT_URL, GROQ_MODEL.
   Frontend: VITE_API_URL, VITE_SOCKET_URL.
10. GitHub "About": description + website link + 10 topics.

------------------------------------------------------------
  ⬜ SIRF 2 CHEEZ BAAKI (tumhari permission chahiye)
------------------------------------------------------------

STEP A — DATABASE (ye bina site kaam nahi karegi)
   /api/health abhi "database":"unreachable" bol raha hai.

   Vercel dashboard → Storage → Create Database → Neon →
   region Singapore (ap-southeast-1) → connect BOTH projects.
   Ya CLI se:  vercel integration add neon

   Phir schema + demo data ek baar daalo:
     cd C:\Users\Admin\Desktop\CLIENTHUB\CLIENTHUB\clienthub\server
     DATABASE_URL="<NEON_URL>" npx prisma db push
     DATABASE_URL="<NEON_URL>" npm run seed

STEP B — BAAKI SECRETS (values server/.env me hain)
     cd server
     vercel env add DATABASE_URL production          # Neon se aayega
     vercel env add JWT_SECRET production            # 32+ random chars!
     vercel env add GROQ_API_KEY production
     vercel env add IMAGEKIT_PUBLIC_KEY production
     vercel env add IMAGEKIT_PRIVATE_KEY production
     vercel env add IMAGEKIT_URL_ENDPOINT production

   Note: JWT_SECRET 32+ chars hona chahiye, warna validateEnv.js
   production me boot fail kar dega.

   Iske baad redeploy:  git commit --allow-empty -m "redeploy" && git push
   Test:  https://clienthub-api-surya.vercel.app/api/health
   Aana chahiye: {"status":"ok","database":"ok",...}

------------------------------------------------------------
  FUTURE ME CHANGES KAISE KARO
------------------------------------------------------------
   Bas GitHub pe push karo — bas itna hi:

     git add -A
     git commit -m "kuch change"
     git push

   Vercel dono project apne aap rebuild + redeploy kar dega.
   Manual deploy ki zaroorat nahi.

------------------------------------------------------------
  DEMO LOGINS (seed ke baad)
------------------------------------------------------------
   Admin:  admin@demo.com  / Demo@1234
   Team:   team@demo.com   / Demo@1234
   Client: client@demo.com / Demo@1234

------------------------------------------------------------
  TROUBLESHOOTING
------------------------------------------------------------
- /api/health pe "database":"unreachable" → DATABASE_URL galat/missing.
- Browser console me CORS error → clienthub-api ka CLIENT_URL
  frontend ke asli URL se match nahi kar raha.
- 401 on login → password Demo@1234 (capital D), ya seed nahi chala.
- Logs:  vercel logs https://clienthub-api-surya.vercel.app
